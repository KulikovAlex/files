<?xml version="1.0" encoding="utf-8"?>
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:rowCount="3" android:columnCount="3"
    android:background="#FFFFFF"
    >

    <ImageButton
        android:id="@+id/play_but"
        android:layout_width="128dp"
        android:layout_height="116dp"
        android:layout_gravity="center"
        android:layout_column="1"
        android:layout_row="1"
        android:background="#FFFFFF"
        android:src="@drawable/ic_launcher_state"
        android:onClick="play"
        />

    <ImageButton
        android:id="@+id/records_but"
        android:layout_width="120dp"
        android:layout_height="90dp"
        android:background="#FFFFFF"
        android:textColor="#FF0000"
        android:layout_column="2" android:layout_row="2"
        android:layout_gravity="end"
        android:src="@drawable/ic_records_state"
        android:onClick="getRecords"
        />

    <ImageButton
        android:id="@+id/info_but"
        android:layout_width="120dp"
        android:layout_height="90dp"
        android:background="#FFFFFF"
        android:textColor="#FF0000"
        android:layout_column="0"
        android:layout_row="2"
        android:layout_gravity="end"
        android:src="@drawable/ic_info_state"
        android:onClick="getInfo"
        />

</GridLayout>

